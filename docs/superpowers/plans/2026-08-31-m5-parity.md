# M5 React Parity 实现计划

- 日期：2026-08-31
- 分支：`codex/m5-parity`（基线 `f61e069`）
- 设计说明：`docs/superpowers/specs/2026-08-31-m5-parity-design.md`
- 纪律：TDD 先行；每任务一个提交；全套验证（vitest + tsc + build + Python 209 基线）
  通过后才提交；仅推送 `backup`；只用冻结契约端点。

## 任务 1：撤销/重做

**文件：** frontend/src/api/client.ts、schemas.ts、types.ts；state/AppStateContext.tsx、
appReducer.ts；components/Toolbar.tsx 及测试。

**步骤：**

1. 红灯（状态层）：undo 成功且 `undone:true` → timeline 重拉 + previewGeneration
   递增 + model 更新；`undone:false` → 静默 no-op（无请求副作用之外的派发）；
   变更 gate 与运行互斥；失败显示结构化错误。
2. 红灯（组件）：Toolbar 「撤销」「重做」按钮触发动作；运行中禁用。
3. 实现解析器（`{undone, redone, model, log}`）与动作；全套验证。
4. 实机：参数编辑 → 运行 → 撤销 → 几何与状态回退（STL rev 变化）。
5. 提交 `feat(M5): 撤销与重做`。

## 任务 2：配方管理（Demo 加载/新建/保存/导出/导入）

**步骤：**

1. 红灯：init 解析 `demo_recipes`/`recipes`；工具栏配方菜单列出；加载 Demo →
   recipe 替换 + preview 刷新；导出下载（blob）；导入（文件选择 → JSON POST）。
2. 实现与实机验证；提交 `feat(M5): 配方管理与导入导出`。

## 任务 3：步骤结构编辑

**步骤：**

1. 红灯：上移/下移/复制/删除/重命名（1–80 字符校验）→ step list 替换、
   statuses 重置语义与旧 UI 一致；键盘可达。
2. 提交 `feat(M5): 步骤结构编辑`。

## 任务 4：掩膜上传与预览

**步骤：**

1. 红灯：Exposure 步骤显示掩膜控件；上传（multipart）成功后步骤参数
   mask_name 更新；预览图加载（preview_step binary）；失败结构化提示。
2. 提交 `feat(M5): 掩膜上传与预览`。

## 任务 5：对等收口

- §2 清单核对、全量回归（前后端 + Golden）、生产式浏览器验收（含新能力）、
  文档更新；提交 `docs(M5): 记录 React Parity 交付`；推送 backup，等待所有者
  审查合并与旧 UI 弃用决定。
