# M7 ProcessBackend 接口实现计划

- 日期：2026-09-01
- 分支：`codex/m7-backend`（基线 `9ec81fb`）
- 设计要点：`ProcessBackend -> VoxelBackend -> ProcessModel`，**行为不变**——
  VoxelBackend 是纯包装（委托 `ProcessStep.execute(model)`），为 M8 ViennaPS
  沙盒与 M9 ViennaPSBackend 预留同语义接缝。能力模型（显式回退）推迟到 M9。
- 纪律：TDD；每任务一提交；全套验证（前端 252 + Python 228 基线 + Golden）；
  仅推送 `backup`。

## 任务 1：接口 + VoxelBackend（行为不变包装）

**文件：** process_backend/{__init__,base,voxel}.py；tests/test_process_backend.py

**步骤：**

1. 红灯：info（name='voxel'/precision='voxel'）；registry（未知名抛
   ProcessBackendError）；快照/恢复回环。
2. 红灯（parity）：Basic Trench @ grid 48 经 VoxelBackend.execute_step 与直接
   `step.execute(model)` 网格逐体素一致；material_surfaces 三角数一致；
   summary 与模型一致。
3. 实现并全套验证；提交 `feat(M7): ProcessBackend 接口与 VoxelBackend`。

## 任务 2：文档与收口

- README（process_backend 用法）、ARCHITECTURE_TARGET（M7 交付）、ROADMAP、spec；
  全量回归；提交 `docs(M7): 记录 ProcessBackend 交付`；推送 backup。
