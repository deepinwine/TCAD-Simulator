# M3 Viewer 完善实现计划

- 日期：2026-08-31
- 分支：`codex/m3-viewer`（基线 `8b2d4c0`）
- 设计说明：`docs/superpowers/specs/2026-08-31-m3-viewer-design.md`
- 纪律：TDD 先行；每任务一个提交；全套验证（vitest + tsc + build + Python 183 基线）
  通过后才提交；仅推送 `backup`。

## 任务 1：透视/正交投影切换

**文件：**

- 修改：frontend/src/viewer/fitCamera.ts（新增 `calculateOrthographicFit`）
- 修改：frontend/src/viewer/fitCamera.test.ts
- 修改：frontend/src/viewer/viewerRuntime.ts（双 camera + `setProjection`）
- 修改：frontend/src/viewer/ThreeViewer.tsx（「透视/正交」按钮，aria-pressed）
- 修改：frontend/src/viewer/ThreeViewer.test.tsx

**步骤：**

1. 红灯：`calculateOrthographicFit` 单测——空/退化 bounds 安全默认；frustum 半高
   ≥ 包围球投影半径；aspect 只影响宽；near<0、far>near。
2. 红灯：runtime 替身测试——`setProjection('orthographic')` 后标准视图仍可用；
   切换保留 target 与方向；重复切换幂等；渲染按需触发。
3. 红灯：组件测试——按钮 aria-pressed 随切换翻转；initError 时禁用；切换不产生
   API 调用（apiCalls 计数不变）。
4. 实现：fitCamera 正交推导 + runtime 双相机换绑 + 按钮。
5. 全套验证 + 浏览器实机（正交下拖拽/缩放/视图按钮、零新增请求）。
6. 提交 `feat(M3): 透视与正交投影切换`。

## 任务 2：X/Y/Z 裁剪平面

**文件：**

- 创建：frontend/src/viewer/clipping.ts + 测试（纯函数：归一化→世界坐标平面）
- 修改：viewerRuntime.ts（localClippingEnabled、材质 clippingPlanes、`setClipping`）
- 修改：ThreeViewer.tsx（三轴开关+滑杆）
- 修改：styles.css

**步骤：**

1. 纯函数测试：三轴法向正确；归一化 0/0.5/1 映射到 min/中点/max；禁用轴不生成平面。
2. runtime 测试：启用即挂平面到全部材质；调整位置更新 Plane 常数；禁用清空。
3. 组件测试：滑杆/开关 aria 语义；操作零 API。
4. 实机：裁剪后网格截面可见（截图确认）；零请求。
5. 提交 `feat(M3): XYZ 裁剪平面`。

## 任务 3：材料显示控制

**文件：**

- 修改：viewerRuntime.ts（`setMaterialDisplay(matId, {visible, opacity})`）
- 创建：frontend/src/viewer/MaterialPanel.tsx + 测试
- 修改：ThreeViewer.tsx / styles.css

**步骤：**

1. runtime 测试：按 matId 定位 mesh；visible 切换；opacity 更新 transparent 标志。
2. 面板测试：列出 manifest 材料名；开关/滑杆 aria；操作零 API。
3. 实机验证 + 提交 `feat(M3): 材料显示控制`。

## 任务 4：选择与测量

**文件：**

- 创建：frontend/src/viewer/picking.ts + 测试（纯函数：屏幕点→ray→命中信息）
- 修改：viewerRuntime.ts（pointerup 无拖拽阈值 → Raycaster；高亮与测量线）
- 修改：ThreeViewer.tsx（信息条 + 测量模式开关 + 距离标签）

**步骤：**

1. picking 纯函数测试（用 fake mesh/相机矩阵）。
2. runtime 测试：点选高亮唯一、再点取消；测量两点出距离；拖拽不触发。
3. 组件 + 实机（点衬底显示 Silicon；两点测量距离与 bbox 尺度一致）。
4. 提交 `feat(M3): 网格选择与两点测量`。

## 任务 5：run/all 网络失败对账

**文件：**

- 修改：frontend/src/state/AppStateContext.tsx（network_error 失败后的 reconcile 动作）
- 修改：components/ErrorNotice.tsx（actionLabel「重新同步」）
- 修改：对应测试

**步骤：**

1. 状态层测试：reconcile 重拉 timeline+init，以服务端状态覆盖 localError。
2. 实机复现 M2 场景（长 run 断连）→ 点「重新同步」→ 状态一致。
3. 提交 `fix(M3): run 网络失败后的状态对账`。

## 任务 6：文档与里程碑收口

- 更新 README（Viewer 能力描述）、ARCHITECTURE_TARGET（M3 交付）、ROADMAP、
  spec 状态；全量前后端回归；生产式浏览器验收（含 M3 新能力）；
  提交 `docs(M3): 记录 Viewer 完善交付`；推送 `backup codex/m3-viewer`，
  等待所有者合并。
